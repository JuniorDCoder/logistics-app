<IfModule mod_rewrite.c>
    RewriteEngine On

    # Route all web requests through Laravel's public directory on shared hosting.
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
